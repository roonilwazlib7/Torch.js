Game = new Torch.Game(~NAME~, 500, 500, ~GRAPHICS~)

Init = (game) ->
    # ...

Load = (game) ->
    # ...

Update = (game) ->
    # ...

Draw = (game) ->
    # ...
Game.Start(Init, Load, Update, Draw);
