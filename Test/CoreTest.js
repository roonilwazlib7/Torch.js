QUnit.test("Torch Core", function( assert ) {
    assert.notEqual(Torch, undefined, "Torch Exists");
    assert.notEqual(Torch.Game, undefined, "Torch.Game Exists");
    assert.notEqual(Torch.Sprite, undefined, "Torch.Sprite Exists");
 });
