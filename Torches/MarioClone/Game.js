var Game = new Torch.Game("canvas", 1000,650, "MarioClone");function Load(){    //Game.Load.Texture("Art/space.svg", "background");    //Game.Load.Sound("Art/Bonus/sfx_laser1.ogg", "shot_sound");    Game.Load.Texture("Art/BasicBlock.png", "BasicBlock");    Game.Load.Texture("Art/QuestionBlock.png", "QuestionBlock");}function Update(){    Spawner.Update();    if (Game.Keys.D.down)    {        Game.Viewport.x -= Game.deltaTime;    }    if (Game.Keys.A.down)    {        Game.Viewport.x += Game.deltaTime;    }    if (Game.Keys.W.down)    {        Game.Viewport.y += Game.deltaTime;    }    if (Game.Keys.S.down)    {        Game.Viewport.y -= Game.deltaTime;    }}function Draw(){}function Init(){    Game.Clear("#6b8cff");    Spawner.Spawn(BlockSpawn);}Game.Start(Load, Update, Draw, Init);Torch.Debug(Game);Game.AllEnemiesAreDead = function(){    var dead = true;    for (var i = 0; i < Game.enemies.length; i++)    {        if (!Game.enemies[i].trash) dead = false;    }    return dead;}